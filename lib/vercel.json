{
  "buildCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b stable flutter-sdk && export PATH=$PATH:$PWD/flutter-sdk/bin && flutter config --enable-web && flutter pub get && flutter build web",
  "outputDirectory": "build/web"
}